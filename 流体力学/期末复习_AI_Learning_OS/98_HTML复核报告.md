# HTML 复核报告

复核时间：2026-04-25  
复核对象：

- `流体力学期末复习_AI_Learning_OS.html`
- `generate_fluid_html.py`
- `01_HTML整合计划.md`
- `98_HTML审核报告.md`

## 结论

**通过。**

上一轮 `98_HTML审核报告.md` 提出的 3 个阻塞项，本轮已确认关闭；当前版本可作为通过复核的 HTML 交付物继续使用。

## 已验证项

### 1. 第 1 章已改为精确读取 `01_流体的物理性质.md`

- `01_HTML整合计划.md:39-41,61-66` 已记录本轮修复目标与重新生成动作。
- `generate_fluid_html.py:305-310` 现用 `ROOT / f"{num}_{title}.md"` 精确拼接章节文件名，不再按前缀模糊匹配。
- `流体力学期末复习_AI_Learning_OS.html:281-295` 的第 01 章正文已出现 `01_流体的物理性质.md` 独有内容，包括：
  - `01 流体的物理性质`
  - `资料依据：... 第一讲流体的物理性质 ...`
  - `连续介质假设`
- 复核中未在生成脚本和最终 HTML 内检出 `01_HTML整合计划` / `流体力学期末复习 HTML 整合计划` / `文件分工` 这些误装配特征串。

结论：**阻塞项 1 已修复。**

### 2. 07 / 09 / 14 章 Markdown 表格中的公式管道符错列问题已修复

- `generate_fluid_html.py:86-142` 新增 `split_table_row()` 与 `table_to_html()`，表格拆分时会避开代码段和数学段内的 `|`。
- 生成后的 HTML 中，原先高风险行已保持为单行单元格，而不是被拆成额外 `<td>`：
  - `流体力学期末复习_AI_Learning_OS.html:1993`：第 07 章保留了 `|\Gamma|`、`4\pi Ua`、`L'=\rho U\Gamma` 相关单元格。
  - `流体力学期末复习_AI_Learning_OS.html:2680`：第 09 章保留了 `|tau|=mu|du/dr|`。
  - `流体力学期末复习_AI_Learning_OS.html:4213`：第 14 章保留了 `delta=|x实-x理|/x理*100%`、`zeta1=(1-A1/A2)^2` 等表达式。
- 额外静态 DOM 校验结果：全页 79 张表的表头列数与表体列数一致，**坏行数 0**。

结论：**阻塞项 2 已修复。**

### 3. 搜索、标签筛选、冲刺模式、补充内容淡化、自测卡片交互已落地

- 顶部控件与标签筛选入口已落地：`流体力学期末复习_AI_Learning_OS.html:93-110`
- 搜索与统一过滤逻辑已落地：`流体力学期末复习_AI_Learning_OS.html:4515-4559`
  - 搜索对象已覆盖 `.chapter`、`.chapter-card`、`.path-card`、公式表、禁用条件、自测卡片。
  - 标签筛选已同时作用于章节卡片和正文章节，不再只筛左侧卡片。
- 冲刺模式已落地：`流体力学期末复习_AI_Learning_OS.html:71-72,4571-4578`
  - 现在只折叠章节详情并保留总索引 `#index`，未再出现上一轮“冲刺模式隐藏总索引”的问题。
- 补充内容默认淡化已落地：`流体力学期末复习_AI_Learning_OS.html:77-79,93,4561-4570`
  - 默认 `body` 带 `hide-supplement`
  - 已从“只淡化 badge”提升为同时淡化对应 `p / li / tr`
- 自测卡片交互已落地：`流体力学期末复习_AI_Learning_OS.html:4491-4494,4584-4589`
  - 点击后会切换答案显示
  - 按钮文案会在“显示答案 / 收起答案”间切换
  - `aria-expanded` 会同步更新

结论：**阻塞项 3 已修复。**

### 4. 兜底 / 拓展内容没有再被强化为主线

- `01_HTML整合计划.md:16-17` 要求“适合冲刺”“补充内容不升格为主线”。
- 当前 HTML 已落实为：
  - 默认淡化补充内容：`流体力学期末复习_AI_Learning_OS.html:77-79,93`
  - 第 14 章拓展内容已明确下沉到 `专业拓展附注（非期末主线）`：`流体力学期末复习_AI_Learning_OS.html:4479-4484`
  - 总索引也明确写出“不把专业拓展当必考”：`流体力学期末复习_AI_Learning_OS.html:269-279`

结论：**主线/补充边界已回到正确状态。**

## 仍需修复项

**无阻塞项。**

本轮复核范围内，没有发现必须拦截交付的剩余问题，因此本次结论维持“通过”。

## 残余风险

1. `html_smoke_test.js` 当前在本机环境下无法直接复跑，因为缺少 `playwright` 依赖；本次行为复核主要依据源码审查、Chrome `--dump-dom` 初始化结果、以及 DOM 结构校验完成，而不是该脚本的完整自动化回归。
2. 补充内容的淡化样式当前主要作用在 `.chapter-body` 内；总索引中的两处 `专业拓展` 提示（`流体力学期末复习_AI_Learning_OS.html:273,279`）仍以普通正文呈现，但文案本身已明确声明“非必考/非主线”，所以不构成当前阻塞。
3. 自测按钮虽然已有 `aria-expanded`，但仍未补 `aria-controls` 与答案区 `id` 绑定（`流体力学期末复习_AI_Learning_OS.html:4492-4494,4584-4589`）；这属于可访问性打磨项，不影响本轮阻塞结论。
